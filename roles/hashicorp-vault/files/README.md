Place the HashiCorp Vault Linux executable in this directory as `vault`, or set
`hashicorp_vault_binary_file` to another filename. Binary artifacts are intentionally
not stored in this source repository.
